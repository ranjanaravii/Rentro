spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/rentro
    username: rentro
    password: rentro123
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  
  redis:
    host: redis
    port: 6379
    timeout: 2000ms

logging:
  level:
    com.rentro: DEBUG
    org.springframework.security: DEBUG
